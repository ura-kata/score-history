import { Grid } from "@material-ui/core";
import React from "react";

export interface UpdateScoreContentProps {}

const UpdateScoreContent = (props: UpdateScoreContentProps) => {
  return (
    <Grid container>
      <Grid item></Grid>
    </Grid>
  );
};

export default UpdateScoreContent;
